function [sortArray] = sort_matrix(A, m, n)
%SORT_MATRIX 此处显示有关此函数的摘要
%   此处显示详细说明
sortArray = zeros(1, m+n);
tmp = zeros(1, m+n);
for i = 1:(m+n)
    tmp(i) = A(i);
end
for i = 1:m+n
    for j = m+n:-1:i
end

